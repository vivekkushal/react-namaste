import Logo from './Logo';
import NavItems from './NavItems';

// Header component
const Header = () => {
  return (
    <div className="header">
      <Logo />
      <NavItems />
    </div>
  );
};

export default Header;
